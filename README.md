Docker Django Template Project

* Copy it
* Start it
  * `docker-compose up`
* Get shell
  * `docker exec -it [container name] bash`
* Run migration
  * `python manage.py migrate`
* Create superuser 
  * `python manage.py createsuperuser --username=joe --email=joe@example.com`
  
